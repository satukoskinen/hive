# 22: Turn Aiur into a slave of the local swarm whose leader is Char
TOKEN=$(docker swarm join-token -q worker)
eval $(docker-machine env Aiur)
docker swarm join --token $TOKEN $(docker-machine ip Char):2377
